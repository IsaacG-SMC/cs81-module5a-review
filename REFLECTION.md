--- REFLECTION ---

Reading through the code made it clear how useful high-order functions are in
making code legible and concise. Unlike the prior code review assignment, which 
had lots of lines to parse and describe, this file felt like it shouldn't need 
many comments. I did write a lot to help ensure I knew what everything was doing
if and when I looked back on this, but I felt like I was jsut repeating what was
already made clear with the names and syntax of all the code.

The arrow functions that were used as values can be a bit confusing still, since
the syntax of the arrow functions themselves still are taking a bit of time getting
used to. As well, the [... new Set(names)] was new to me, and I had to look up what
sets are and how they function in comparison/relation to arrays.
